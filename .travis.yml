language: cpp
script: cd cpp && make
matrix:
  include:
  - os: linux
    addons:
      apt:
        sources:
        - ubuntu-toolchain-r-test
        packages:
        - g++-6
    before_install:
    - eval "CXX=g++-6"
  - os: linux
    compiler:
    - clang
